.DS_Store
node_modules/

# Runtime state generated by patches/self-heal.sh — machine-specific, not template content
patches/.last-healed-version
patches/.self-heal-work/

# If you run pi from inside a clone of this repo while testing, these are
# cycle/session artifacts, not harness source
data/
sessions/
